# 项目修改日志

## 2025-09-21 22:00 - 修复项目启动问题

### 问题描述
项目在从 `cmd` 目录运行 `go run main.go run` 时出现以下错误：
```
2025/09/21 21:55:20 加载模型文件失败: open conf\model.conf: The system cannot find the path specified.
2025/09/21 21:55:20 Casbin enforcer 初始化失败
exit status 1
```

### 问题分析
1. 当从 `cmd` 目录运行时，程序工作目录是 `cmd`，但配置文件在项目根目录的 `conf` 文件夹中
2. 代码中使用相对路径 `"conf"` 查找配置文件，导致路径错误
3. `server_judge.go` 中硬编码了配置文件路径 `"conf/config.yaml"`，同样存在路径问题

### 修复方案

#### 1. 修复 `cmd/main.go` 中的 Casbin 配置路径问题
- 添加智能路径检测，支持从 `cmd` 目录或项目根目录运行
- 修改代码：
```go
// 获取配置目录路径，支持从cmd目录或项目根目录运行
configDir := "conf"
if _, err := os.Stat(configDir); os.IsNotExist(err) {
    // 如果当前目录没有conf，尝试上级目录
    configDir = "../conf"
}
```

#### 2. 修复 `pkg/app/api-server/server/server_judge.go` 中的配置文件路径问题
- 添加路径检测和错误处理
- 添加必要的导入：`"log"` 和 `"os"`
- 修改 `NewJudge()` 函数：
```go
configPath := "conf/config.yaml"
if _, err := os.Stat(configPath); os.IsNotExist(err) {
    // 如果当前目录没有conf，尝试上级目录
    configPath = "../conf/config.yaml"
}

cfg, err := utils.LoadConfig(configPath)
if err != nil {
    log.Printf("加载配置文件失败: %v", err)
    // 返回默认配置的Judge
    return &Judge{
        choice: utils.LocalJudge,
        svc:    service.NewService(),
    }
}
```

#### 3. 修复运行命令
- 原来只运行 `go run main.go run` 只编译了 `main.go`，没有包含 `runServer.go`
- 正确的运行命令：`go run main.go runServer.go run`

### 修复结果
✅ 项目现在可以成功启动
✅ Casbin 初始化成功
✅ 本地判题启动成功
✅ 所有 API 路由注册成功
✅ 服务器在端口 8080 上正常监听
✅ API 接口正常响应（需要认证令牌）

### 支持的运行方式
1. 从项目根目录：`go run cmd/main.go run`
2. 从 cmd 目录：`go run main.go runServer.go run`

### 测试验证
- 使用 `netstat -an | findstr :8080` 确认服务器监听端口 8080
- 使用 `Invoke-WebRequest` 测试 API 接口，返回正常的认证错误信息，证明服务正常运行

## 2025-09-21 22:15 - 修复MongoDB认证问题

### 问题描述
项目运行时出现MongoDB认证错误：
```
timestamp="2025-09-21T22:13:48Z" level=error msg="MongoFindErr{error 26 0  (Unauthorized) Command find requires authentication} {table 15 0 submits <nil>} {query 23 0  map[status:1 stime:map[$gt:0]]}"
```

### 问题分析
1. MongoDB服务器(42.194.245.236:27017)启用了认证
2. 当前配置文件中只有基本的连接URI，没有包含用户名和密码
3. 需要在连接字符串中添加认证信息

### 修复方案

#### 1. 扩展MongoDB配置结构
- 修改 `pkg/utils/yaml.go` 中的 `MongoConfig` 结构体：
```go
type MongoConfig struct {
    Uri      string `yaml:"Uri"`
    DbName   string `yaml:"DbName"`
    Username string `yaml:"Username"`
    Password string `yaml:"Password"`
    AuthDB   string `yaml:"AuthDB"` // 认证数据库，默认为admin
}
```

- 同步更新 `conf/config.go` 中的配置结构

#### 2. 添加MongoDB URI构建函数
- 在 `pkg/utils/mongo.go` 中添加 `BuildMongoURI` 函数
- 支持自动构建带认证的MongoDB连接字符串
- 处理URL编码和认证数据库设置

#### 3. 更新DAO层连接逻辑
- 修改 `pkg/dao/dao.go` 中的 `NewDao()` 函数
- 使用新的URI构建函数创建MongoDB连接

#### 4. 更新配置文件
- 在 `conf/config.yaml` 中添加MongoDB认证配置项：
```yaml
Mongo:
  Uri: mongodb://42.194.245.236:27017
  DbName: zkoj
  Username: ""  # MongoDB用户名，如果需要认证请填写
  Password: ""  # MongoDB密码，如果需要认证请填写
  AuthDB: "admin"  # 认证数据库，默认为admin
```

### 使用说明
1. **无认证环境**：保持 `Username` 和 `Password` 为空，系统会使用原始URI
2. **需要认证环境**：填写正确的用户名和密码，系统会自动构建认证URI
3. **自定义认证数据库**：可以修改 `AuthDB` 字段指定认证数据库

### 下一步操作
需要联系MongoDB管理员获取正确的用户名和密码，然后更新配置文件中的认证信息。

## 2025-09-21 22:30 - AI测试用例生成功能设计文档梳理

### 工作内容
完成了AI测试用例生成功能的完整设计文档梳理，包括：

#### 1. 功能设计文档 (`da/AI测试用例生成功能设计文档.md`)
- **功能概述**：明确了AI驱动的测试用例生成功能目标和业务价值
- **需求分析**：详细分析了功能需求和非功能需求
- **系统架构设计**：设计了分层架构和包结构
- **接口设计**：定义了REST API和内部接口
- **数据模型设计**：设计了MongoDB数据模型
- **配置设计**：扩展了AI相关配置
- **实施计划**：制定了4个阶段的开发计划
- **风险评估**：识别了技术和业务风险及缓解措施

#### 2. 技术实现方案 (`da/AI测试用例生成技术实现方案.md`)
- **技术选型**：选择阿里云千炼作为主要LLM，Go作为开发语言
- **核心技术实现**：
  - AI客户端设计模式（策略模式+工厂模式+适配器模式）
  - 提示词工程（模板系统+动态构建）
  - 响应解析策略（多层解析+容错机制）
  - 质量验证机制（多维度验证+验证规则）
  - 缓存策略（多级缓存）
  - 错误处理和重试机制（指数退避+熔断器）
- **性能优化策略**：并发处理、流式处理、资源池管理
- **监控和日志**：指标收集、结构化日志
- **安全考虑**：API密钥管理、请求验证
- **测试策略**：单元测试、集成测试、性能测试

#### 3. API接口规范 (`da/AI测试用例生成API接口规范.md`)
- **接口概述**：定义了基础URL、认证方式、响应格式
- **核心接口**：
  - 生成测试用例 (`POST /api/v1/testcase/generate`)
  - 验证测试用例 (`POST /api/v1/testcase/validate`)
  - 查询生成历史 (`GET /api/v1/testcase/history`)
  - 获取生成详情 (`GET /api/v1/testcase/generation/{id}`)
  - 导出测试用例 (`GET /api/v1/testcase/export/{id}`)
- **管理接口**：LLM提供商列表、使用统计
- **错误处理**：完整的错误码定义和错误响应格式
- **接口调用示例**：JavaScript和Python示例代码

### 架构设计亮点

#### 1. 解耦设计
- **AI工具包**：通用的AI能力抽象，不与具体业务耦合
- **业务包**：专门的测试用例生成业务逻辑
- **清晰分层**：API层、服务层、AI工具层、数据层分离

#### 2. 可扩展性
- **多LLM支持**：通过接口抽象支持多个LLM提供商
- **插件化验证**：验证器可插拔，易于扩展新的验证规则
- **模板化提示词**：提示词模板化管理，便于优化和维护

#### 3. 高可用性
- **重试机制**：指数退避重试和熔断器保护
- **多级缓存**：内存、Redis、数据库三级缓存
- **错误恢复**：完善的错误处理和降级策略

#### 4. 质量保证
- **多维验证**：语法、逻辑、答案、约束多维度验证
- **质量评估**：覆盖度、区分度、难度等多指标评估
- **持续优化**：基于反馈的提示词和策略优化

### 下一步计划
1. **开发环境准备**：配置开发环境和依赖
2. **AI工具包开发**：实现通用AI客户端和提示词工具
3. **业务逻辑开发**：实现测试用例生成、验证、评估逻辑
4. **API接口开发**：实现REST API和服务层
5. **集成测试**：端到端功能测试和性能测试